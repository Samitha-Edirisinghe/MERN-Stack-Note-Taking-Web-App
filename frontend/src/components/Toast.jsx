// This component is not needed separately because we use react-hot-toast directly.
// Keeping a placeholder if you want custom toast logic.
export const showToast = (message, type = 'success') => {
  // Implementation can be added here if needed
};